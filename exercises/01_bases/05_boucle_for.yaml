id: "sum_range"
title: "Somme d'une plage"
description: |
  Crée une fonction sum_range(n) qui calcule la somme de tous les nombres de 1 à n.

  Utilise une boucle for pour additionner les nombres.

  Exemples :
  - sum_range(5) → 15  (car 1 + 2 + 3 + 4 + 5 = 15)
  - sum_range(10) → 55  (car 1 + 2 + ... + 10 = 55)

template: |
  def sum_range(n):
      # Utilise une boucle for
      pass

hints:
  - "Initialise une variable total = 0"
  - "Utilise for i in range(1, n+1): pour aller de 1 à n"
  - "Additionne chaque nombre au total : total += i"
  - "Retourne le total à la fin"

tests:
  - input: [5]
    expected: 15
    description: "Somme de 1 à 5"
  - input: [10]
    expected: 55
    description: "Somme de 1 à 10"
  - input: [1]
    expected: 1
    description: "Un seul nombre"
  - input: [0]
    expected: 0
    description: "Zéro"
  - input: [100]
    expected: 5050
    hidden: true
    description: "Grande plage"
